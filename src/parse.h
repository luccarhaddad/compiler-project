#ifndef _PARSE_H_
#define _PARSE_H_

#include "globals.h"

/**
 * Parses the input and constructs a syntax tree.
 *
 * @return A pointer to the root of the syntax tree.
 */
TreeNode* parse(void);

#endif
